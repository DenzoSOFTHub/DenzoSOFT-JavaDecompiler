# BUG-2026-0080: Java construct-matrix decompiler gaps

**Type**: Bug / Improvement
**Status**: IN_PROGRESS (matrix 28→35; remaining are hard/erasure-bound)
**Severity**: HIGH
**Created**: 2026-06-09

## Description
A comprehensive Java 1.0–25 construct matrix (55 self-contained classes, strict decompile→recompile) revealed
27 failing classes (baseline 28/55). See `docs/reports/report-coverage-assurance.md`. Breadth test on 1674 real
JDK 25 classes: 0 crashes, 93.3% marker-clean.

## Solution (batch 1 — cleared C_TextBlocks, C_Arrays, C_AnnotationDecl, C_AnnotationUse)
- **SHARED-1**: `JavaSourceWriter.writeExpressionSimple` dropped invocation/new arguments inside array
  initializer elements (`(/* ... */)`). Delegate those bounded node kinds to the full `writeExpression`.
- **RC-5 multianewarray**: keep the FULL array type on the `NewArrayExpression` (was unwrapping one
  dimension → `new int[][d1][d2]` + `int[][] = <int[]>` type mismatch).
- **Anno 2a**: suppress the implicit `Annotation` superinterface on `@interface` declarations
  (`@interface X extends Annotation` is illegal) — top-level + inner.
- **Anno RC-4**: route the annotation type through `emitRef` so a nested annotation type renders as
  `Outer.Inner` (was the bare `Inner`, unresolved).

### Files Modified
- `service/writer/JavaSourceWriter.java`, `service/converter/ClassFileToJavaSyntaxConverter.java`

### Result
Construct matrix 28/55 → 32/55; corpus holds at 25; DecompilerTest 32/33; regression 159 classes 0 crashes.

## Remaining (tracked)
Lambda/raw-functional-interface generic inference (C_FunctionalInterfaces, C_VarLambdaParams, C_Optional),
return-across-synchronized (C_Concurrency), record canonical-ctor + ternary-arg (C_Records), generic method
signatures + bridge methods (C_RecursiveGenerics, C_Generics), guarded pattern switch (C_PatternSwitch),
unnamed `_` (C_Unnamed), local classes (C_InnerClasses).

## Solution (batch 2-4 — NaN, record ctor, array-foreach type, for-var scope, catch-var recursion)
- B-rec: emit `Double.NaN`/`POSITIVE_INFINITY` (no literal form).
- D-rec: emit the access modifier on a record compact canonical constructor.
- RC-6: array-foreach element type from the array component type.
- For-var used after the loop kept declared outside (empty for-init).
- RC-8: catch-var rename recurses into NewExpression/Ternary/Unary/FieldAccess args.

Matrix 33/55; corpus 25; DecompilerTest 32/33; regression 159 classes 0 crashes.

## Remaining clusters (hard / deferred)
- Lambda/raw-functional-interface + generic-local types: ERASURE-bound without `-g` (parity with JD-Core; with `-g` they recompile). Needs parameterized-type AST support to synthesize from the invokedynamic SAM.
- Switch/control-flow var declaration-hoisting (var assigned in switch cases, used after).
- Guarded pattern switch (`when`), unnamed `_` (Java 22+).
- Anonymous-class capture (`val$`), local classes, finally-unreachable.

## Solution (batch 5-6 — switch-var hoisting + boolean-arg simplification)
- SwitchVarHoister: hoist a var declared in a switch case but read in another case/after the switch.
  (also dropped the original 13-class corpus 25->21).
- BooleanSimplifier recurses into invocation/new ARGUMENTS so `boolMethod() != 0` inside a call/ternary arg
  is reduced.

Matrix 35/55; corpus 21; DecompilerTest 32/33; regression 160 classes 0 crashes.

## 2026-06-10: full verified gap analysis — umbrella now decomposed into dedicated items

Matrix extended to **57 classes** with Java 25 constructs: C_FlexibleCtor (JEP 513 flexible
constructor bodies) and C_ImplicitMain (JEP 512 compact source files). Baseline **37/57** clean.

A 63-agent verified analysis (adversarial verification on every root-cause claim + semantic audit
of all recompile-clean classes + open-bug re-verification) produced 45 confirmed root-cause
findings, **11 silent miscompilations** (5 CRITICAL) on classes that recompile cleanly, and the
complete prioritized plan in `docs/reports/report-construct-matrix-gap-analysis.md`
(5 phases, expected end state 57/57).

The previous "Remaining 20" list is superseded — every cluster now has a dedicated item:

| Fix order | Cluster | Item |
|---|---|---|
| 1 | super-call-virtualized (CRITICAL silent) | BUG-2026-0081 |
| 2 | iinc-postfix-expression (CRITICAL silent) | BUG-2026-0082 |
| 3 | ternary-value-merge (CRITICAL silent; absorbs 0053, 0069c) | BUG-2026-0083 |
| 4 | wide-opcode-decode | BUG-2026-0084 |
| 5 | switch-break-emission (silent fall-through) | BUG-2026-0085 |
| 6 | loop-exit-branch (silent body loss) | BUG-2026-0086 |
| 7 | dup-store-alias (silent double-read) | BUG-2026-0087 |
| 8 | textblock-trailing-ws (silent constant change) | BUG-2026-0088 |
| 9 | record-canonical-ctor (silent validation loss) | BUG-2026-0089 |
| 10 | annotation-metadata (silent reflection loss) | BUG-2026-0090 |
| 11 | finally-dedup | BUG-2026-0091 |
| 12 | synchronized-reconstruction | BUG-2026-0092 |
| 13 | cast-precedence | BUG-2026-0093 |
| 14 | signature-unread | BUG-2026-0094 |
| 15 | sealed-permits (prereq for 16) | BUG-2026-0071 |
| 16 | pattern-switch-folding | BUG-2026-0067 |
| 17 | compound-cond-stmt-drop | BUG-2026-0095 |
| 18 | switch-expression-reconstruction | BUG-2026-0066 |
| 19 | twr-reconstruction | BUG-2026-0068 |
| 20 | erasure-generics (staged A/B/C) | BUG-2026-0069 + report §3 |
| 21 | local-slot-typing | BUG-2026-0096 |
| 22 | inner-classes-captures | BUG-2026-0097 |
| 23 | requirenonnull-leak (cosmetic) | BUG-2026-0098 |
| — | module-info requires flags (separate sweep) | BUG-2026-0099 |

Expected matrix trajectory per the report: Phase A→39, B→43, C→48, D→55, E→57/57.
