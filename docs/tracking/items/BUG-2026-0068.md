# BUG-2026-0068: Modern (Java 9+) try-with-resources not collapsed

**Type**: Bug
**Status**: IN_PROGRESS (typed-resource collapse resolved)
**Severity**: HIGH
**Created**: 2026-06-08

## Description
TWR desugar without a synthetic finally is not recognised: resources retyped to Object, double close, undefined throw variable. Corpus J9Interfaces, ClassicConstructs. cf BUG-2026-0054/0056.

See `docs/reports/report-decompilation-defect-analysis.md` (defect F09) for full root-cause, file:line and
suggested approach.

## Solution Implemented (partial)
New `transform/ModernTwrReconstructor` collapses the Java 9+ desugar
`R r = init; try { body } catch (Throwable t) { r.close(); throw t; } r.close();` into
`try (R r = init; ...) { body }`. Wired after `TryCatchReconstructor`. The import collector now also scans
try-resource declarations (they were lost when the decls moved into the resource header).

### Files Modified
- `service/converter/transform/ModernTwrReconstructor.java` (new)
- `service/converter/ClassFileToJavaSyntaxConverter.java` (wiring)
- `service/writer/JavaSourceWriter.java` (import collection for try-resources + finally)

### Result
`ClassicConstructs.tryWithResources` -> `try (BufferedReader var2 = ...; BufferedWriter var3 = ...) { ... }`.

### Remaining
The effectively-final form (`try (a; b)` over pre-existing vars) is mangled at the flow-builder level
(undefined throw variable, `if (var==null)` close chains) and is NOT collapsed — needs deeper exception-graph
handling. Tracked here.


## Re-verification (2026-06-10, construct-matrix gap analysis)

**Still reproduces**: YES

Partially fixed, matching the item's own IN_PROGRESS status. Fixed: single typed resource (try (R r = init) {...}) collapses correctly (readFirst, useCustom). Still broken: (1) multi-resource typed form — only the outermost resource collapses; the inner one stays as the raw desugar with an unreachable trailing close (the item claimed multi-resource worked for ClassicConstructs, but it does NOT collapse when the body returns from inside the try); (2) effectively-final form (try (a; b)) — fully mangled exactly as the item's "Remaining" section describes: Object retype, use-before-declaration close variable, null-check close chains; (3) matrix readOrDefault shows TWR+catch also still mangled (resource declared inside a spurious inner try, used out of scope). Item file is at /workspace/DenzoSOFT-JavaDecompiler/docs/releases/v1.9.0/BUG-2026-0068.md (already moved to the v1.9.0 release dir despite IN_PROGRESS status). Repro artifacts under /tmp/bugverify/BUG-2026-0068/ (Twr.java, dec/Twr.java, iso/Twr.java).

**Cross-reference**: Cluster twr-reconstruction, fix order #19; absorbs BUG-2026-0054 residual scoping shapes.
