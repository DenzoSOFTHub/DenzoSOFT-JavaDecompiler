# BUG-2026-0071: sealed/permits/non-sealed dropped on nested types

**Type**: Bug
**Status**: TO_ANALYZE
**Severity**: LOW
**Created**: 2026-06-08

## Description
Nested sealed types lose their `sealed`/`permits`/`non-sealed` modifiers (fidelity only; still compiles). Corpus J17Sealed.

See `docs/reports/report-decompilation-defect-analysis.md` (defect F08) for full root-cause, file:line and
suggested approach.


## Re-verification (2026-06-10, construct-matrix gap analysis)

**Still reproduces**: YES

Symptom is unchanged in shape: exactly as described (sealed/permits/non-sealed silently dropped on nested types; output still recompiles, so fidelity-only). Bookkeeping anomaly: the item file is not in docs/tracking/items/ — it sits in docs/releases/v1.9.0/BUG-2026-0071.md with Status still TO_ANALYZE, i.e. it was swept into the v1.9.0 release directory without a Solution Implemented section, yet the defect is not fixed. Root-cause site per report-decompilation-defect-analysis.md defect F08: JavaSourceWriter.writeInnerClass never reads isSealed() and non-sealed is never derived.

**Cross-reference**: Cluster sealed-permits, fix order #15 — PREREQUISITE for BUG-2026-0067 (exhaustive switches need a sealed hierarchy to recompile).
